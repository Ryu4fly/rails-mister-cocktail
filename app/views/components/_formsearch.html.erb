<%= simple_form_for :search, url: cocktails_path, method:"GET", html: { class: 'form-inline searchbar' } do |f| %>
  <%= f.input :name, input_html: {value: "", id: 'search'}, required: true, label: false %>
  <%= f.submit "Search", class: "btn btn-primary search-submit" %>
<% end %>

