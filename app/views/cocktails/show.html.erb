<h1>Featuring <%= @cocktail.name %></h1>

<div class="info">

  <% @cocktail.doses.each do |dose| %>
    <p><%= dose.description %></p>
    <p><%= dose.ingredient.name %>   <%= link_to '🗑', dose_path(dose),
    method: :delete,
    data: { confirm: "Are you sure? "} %></p>
    <hr>

  <% end %>

</div>
